import { VisierPermissionManagement } from "./index";

describe("visier-permission-management-typescript-sdk", () => {
    it("initialize client", async () => {
        const visierpermissionmanagement = new VisierPermissionManagement({
        });
    });
});
